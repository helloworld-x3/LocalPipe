# LocalPipe 效果指标面板

> 定位：服务作品文档"可落地的效果"（教练五问第⑤问）。六项指标把"AI 看起来不错"转成"质量、风险、效率可以被测量"。
> 数据口径：**所有数字标注来源与样本量**。样本不足处明示"待补"，不做无依据宣称。数据是活的——跑 `python stats.py` 和本文档附录脚本刷新。

## 六项核心指标

| 指标 | 当前数值 | 样本量 | 数据来源 | 证明什么 |
|---|---|---|---|---|
| 加权保真率 | 0.92（fashion demo） / 1.00（coolclip demo） | 2 条 demo | `outputs/demo_fr_fashion_package.json`、`demo_fr_coolclip_package.json` | 核心事实和卖点是否保留 |
| 风险拦截率 | C04 定量宣称被拦截 → needs_review；真实调用 39.2% 进 needs_review、3.9% error | n=153 | `.cache/telemetry.jsonl` | 不适合发布的表达是否被识别 |
| 母语者胜率 | 待补（n=1 pilot，不足以算胜率） | n=1 评审 × 3 条 | `france_native_survey_B.pdf` | 是否比裸 Prompt 更自然 |
| 发布可用率 | 6/6 评价为"可发布/小幅修改"（A+B） | n=1 评审 × 3 条 × X/Y | 同上 | 能否减少人工重写 |
| 平均处理时间 | 真实调用中位 8.4s，均值 15.3s（p90 22.9s） | n=153 | `.cache/telemetry.jsonl` | 是否提升生产效率 |
| 画像置信度变化 | fr v0.1→v0.2；母语者反馈独立支持 fr-002 | 10 条条目 | `profiles/fr.json` + 问卷 | 系统能否按反馈迭代 |

---

## 1. 加权保真率

**定义**：程序按要素类型重算的保真率，不信任 LLM 自报。权重：品牌保护词与含数字的产品事实 = 3，核心卖点 = 2，情绪钩子与行动号召 = 1。公式：`Σ(权重 × 保留) / Σ(权重)`。

**当前值**：
- fashion demo：0.92（未加权 0.86）——丢失的是低价值情绪词，加权后损失变小。
- coolclip demo：1.00——数字事实"3 秒降温 15 度"完整保留。

**为什么这样设计**：漏掉一个数字事实比少一个情绪词严重得多。数字卖点丢失时加权率明显低于简单平均（例：`3秒降温15度` 丢失 → 0.40 vs 0.50），品牌词丢失同理。它把"广告业务直觉"编码进质检，而不是对所有要素一视同仁。

**待补**：批量运行后按市场汇总，给出分布而非单点 demo。

## 2. 风险拦截率

**定义**：不适合发布的表达是否被识别。两条证据链：
- **案例**：CoolClip"3 秒降温 15 度"在法国画像下触发 fr-002 定量功效宣称禁忌 → needs_review。C04"瘦十斤"身材宣称同样被拦截。
- **统计**：真实调用中 39.2% 进 needs_review（保真/禁忌/画像引用任一未过即进），3.9% error。**LocalPipe 不直接放行，多一层人工复核门**——这正是广告合规需要的：宁可慢，不可错。

**待补**：禁忌条目命中分布（fr-002 占比 vs 其他）；正式投放拒审率（当前无投放数据，ROI 列为后续目标）。

## 3. 母语者胜率

**口径**：非平局胜率 `W = LocalPipe 胜 / (胜 + Baseline 胜)`，平局剔除。样本少不做显著性宣称。

**当前**：**待补**。已回收 n=1 评审者 pilot（3 条 × X/Y），不足以算胜率。方法学已锁定：先验能力 → 正式问卷 → 揭盲对照表（X/Y→方法）→ 按市场汇总。止损线 8/11，届时数据不足则如实写"探索性报告"。

## 4. 发布可用率

**定义**：问卷 Q4 中"原样发布（A）+ 小幅修改（B）"占有效评价的比例。

**当前值**：6/6 = 100%（A=1，B=5，C=0，D=0）。注意 FR03 含"-5kg"宣称的版本被判 B（小幅修改）而非 A——**风险案例在发布可用率上同样体现**，与指标 2 呼应。

**待补**：评审量扩大后给出 C/D 比例；记录真实人工修改分钟数，计算时间节省率 `(T人工 − T LocalPipe) / T人工`。

## 5. 平均处理时间

**口径**：真实 LLM 调用（排除缓存命中，缓存会把均值拉成假数字）。中位比均值更代表"正常调用"——均值 15.3s 被 204s 的异常调用拉高。

**当前值**（n=153）：中位 **8.4s**，p90 22.9s。分层：解构 1.3s / 重创作 5.4s / 保真回检 2.2s / 禁忌 1.1s。

**证明什么**：一次四层管线（含打回重试闭环）8 秒级完成——人工创作一条本地化广告通常以小时计，素材生产环节以此为基准。

**待补**：与人工基线对比的实测记录（找测试者实际计时，不估算）。

飞书真实任务运行后执行：

```text
python feishu_connector.py --export-metrics outputs/feishu_business_metrics.json
```

导出只统计审核表中明确填写的配对数据：AI 总耗时、人工审核分钟数、人工从头完成基线分钟数。任一字段缺失，该条不会进入时间节省统计，避免用估算值冒充企业实测。

## 6. 画像置信度变化

**定义**：画像不是写死的知识库，而是带元数据的可校准证据系统。fr 条目含 `evidence_level`（A 权威源 / B / C 冷启动假设）、`validation_status`、`unverified_claims`、`expires`。

**当前值**：
- fr 画像 v0.1 → v0.2（2026-08-08），10 条条目。
- fr-002（法国广告避免体重/数字功效宣称）：evidence_level A（ARPP 官方规则引用）+ **母语者独立验证**——FR03 评审主动指出"-5kg"宣称不适合法国广告，与管线拦截结论一致。

**为什么重要**：这是"系统能按反馈迭代"的证据——母语者反对票（s/f 计数）将驱动条目置信度更新，实现贝叶斯式校准。当前母语者票数极少（n=1），明示为"初步校准信号"。

---

## 附录：如何刷新本面板

处理时间 / 拦截率：`python stats.py` 或直接读 `.cache/telemetry.jsonl` 全量统计（stats.py 默认只看最近 20 条，文档数字用全量 n=153 口径）。
demo 保真率：`python generate_demo_fr_fashion.py && python generate_demo_fr_coolclip.py`（命中缓存，不新增 LLM 花费）。
画像元数据：`profiles/fr.json`。
母语者问卷：唐启程渠道回收，揭盲后更新指标 3/4。
